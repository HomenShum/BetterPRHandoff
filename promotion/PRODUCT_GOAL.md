# Product goal — BetterPRHandoff

## Who opens this, and what they are trying to finish

Someone has just finished a week of changes and is about to hand the branch to
somebody else — a reviewer, a teammate taking the project over, themselves in
three months, or the next AI coding assistant that opens the repo. They arrive
with one job: leave behind a record the next reader can follow without
reconstructing it from forty commit messages. Right now that record does not
exist. The person picking up the branch opens the commit history, sees "fix",
"wip", "address feedback", and has no way to answer a plain question like *what
has the checkout screen looked like over the last six months, and why did it
change*. This repo hands them a protocol and a small command-line tool that
scaffolds it: one dated, append-only file per screen, component, server module,
database table, integration and script (a **per-surface changelog lane**), plus
templates for a recorded demo and a reviewer hand-off packet. When it worked,
they walk away holding a `CHANGELOG/` folder of per-surface history committed
next to the code, and an instructions file their own coding assistant reads
before every commit so that history keeps itself current. **BetterPRHandoff does
not run an agent for anyone — it is rules and scaffolding that the user's
existing agent follows.**

## The gate

This repo is judged by the twelve-condition PROMOTION gate, which lives in one
place and is not restated here:

**https://github.com/HomenShum/NodeKit/blob/main/templates/promotion/GATE.md**

Gate variant: `reduced` <!-- reduced = library/CLI judged on its demo
surface and quickstart; see the GATE's reduced-gate section -->

Scoring vocabulary is PASS / FAIL / **UNVERIFIED**, and UNVERIFIED is never PASS.

### Which surface each condition was scored against

This package has two surfaces a stranger actually meets, and they are not the
same shape. The split is stated here so the scoring below is auditable rather
than convenient:

- **CLI quickstart** (`bin/init.mjs` — `init`, `add`, `qa-init`, `qa`,
  `install`) — the surface the README's install block sends you to. Scored:
  conditions 5, 10, 11.
- **The one browser-rendered artifact the product emits** —
  `QA_DOGFOOD/<feature-id>/gmail-magic-resend.html`, written by
  the CLI's `qa <feature-id>` from `templates/gmail-magic-resend.html`. It is
  the only HTML a user of this package ever opens. Scored: conditions 3, 4, 6, 9.
- Conditions 1 and 2 span every journey in
  [PRODUCT_JOURNEYS.md](PRODUCT_JOURNEYS.md), on whichever surface each runs.
- Conditions 7, 8 and 12 were the three the baseline could not run at all. All
  three were run in iteration 3 against the rendered surface, and their evidence
  is in [`promotion/evidence/`](evidence/) with the producer beside it.

Every row below names an artifact under [`promotion/evidence/`](evidence/), and
every one of those artifacts is regenerated by a single committed command:
`node promotion/evidence/audit.mjs`. That is the gate's both-halves rule — output
committed, producer committed — and it is why no row here cites a number that
only existed inside an agent session.

## Canonical journeys

The work queue lives in [PRODUCT_JOURNEYS.md](PRODUCT_JOURNEYS.md). A journey
without browser evidence is unfinished, however green the tests are.

## Loop state

Every iteration is recorded in [PROMOTION_LOG.md](PROMOTION_LOG.md) — journey
exercised, defect fixed, evidence path, conditions newly passing. Loop state
lives in git, never in an agent's memory, so any agent can resume the loop cold.

## Current scorecard

Iteration 3, 2026-08-13. The four UNVERIFIED rows the baseline left behind are
gone — every one was run, not reasoned about — and the rows that were already
PASS were re-measured through a committed producer, because their original
evidence was a browser session nobody else can reopen. Two rows are still FAIL,
and they are FAIL for a reason a reader can reproduce in one command.

| # | Condition | Status | Evidence / reason |
|---|-----------|--------|-------------------|
| 1 | Journeys succeed end-to-end in a real browser | FAIL | J1, J2 and J3 pass at the CLI; J2's D1 reproduction was closed in iteration 2. **J4 now passes in a real browser** at 1440, 768 and 375 — [`evidence/screenshot-mobile-375.png`](evidence/screenshot-mobile-375.png) and the three width blocks in [`evidence/surface-measurements.json`](evidence/surface-measurements.json). Still FAIL because **J5 cannot be driven at all**: the three files it names (`templates/recorder.mjs`, `verifier.mjs`, `probe-routes.mjs`) were deleted in wave 3 and never replaced. `git ls-files templates/ \| grep mjs` returns nothing. New defect **D8**. |
| 2 | No critical or major usability defect open | FAIL | D1 closed (iteration 2). **D2 closed this pass** and re-measured, not assumed. Still FAIL on **D8** (major): README lines 15, 70 and 109 list "verified demo recording — Playwright recorder + Gemini video analysis, both must pass" as one of the three things this skill delivers, and no recorder ships. The disclosure exists, 68 lines further down at line 177, which is not where the promise is read. Minors D5 and D7 remain open. |
| 3 | Mobile and desktop both intentional | **PASS** | Was FAIL on D2. Fixed by adding `<meta name="viewport" content="width=device-width, initial-scale=1">` and re-measured at 375×812 `isMobile`: `clientWidth` **980 → 375**, `visualViewport.scale` **0.383 → 1**, table text **4.97 → 13** effective px. Three screenshots at 1440 / 768 / 375 in [`evidence/`](evidence/). Zoom is not disabled — no `maximum-scale`, no `user-scalable=no`. |
| 4 | No horizontal overflow at supported widths | PASS | Re-measured with a retained producer, which the baseline's version was not. `scrollWidth − clientWidth` is **0 px** at 1440, 768 **and** 375. The 1 px the baseline recorded at mobile was an artifact of the 980 px legacy layout and went away with row 3. |
| 5 | Loading/empty/success/error/agent-running designed | PASS | CLI surface, and now protected rather than hand-run: the eight distinct states (`init` success, `init` refusing to clobber, `add`, `add` duplicate, bad category, `qa` with no argument, `qa` duplicate, `install nonsense`) are asserted by `test/cli.test.mjs`, whose output is committed at [`evidence/npm-test.txt`](evidence/npm-test.txt) — 20 pass, 0 fail. The browser surface's empty state is labelled in place ("Fill links after capture…", "before / after / diff pending"). Loading and agent-running have no subject: every verb is synchronous file scaffolding and the product runs no agent. |
| 6 | Keyboard and basic accessibility pass | **PASS** | Was UNVERIFIED because `Tab` never reached the page. Driven with Playwright instead of the shared browser pane: **8 `Tab` presses, 7 landing on an interactive element**, DOM order, every one reporting `matchesFocusVisible: true` and `outline: auto 1px rgb(16, 16, 16)`. Keypress→focus 3–12 ms. axe-core 4.13.0 over the same page: **0 violations, 23 rules passing**. `keyboard[]` in [`evidence/surface-measurements.json`](evidence/surface-measurements.json), [`evidence/axe.json`](evidence/axe.json). |
| 7 | Web Interface Guidelines: no major unresolved | **PASS** | Was UNVERIFIED. Reviewed against the Vercel Web Interface Guidelines fetched from <https://vercel.com/design/guidelines>, section by section, in [`evidence/WIG_REVIEW.md`](evidence/WIG_REVIEW.md). Two major findings — "Respect zoom / Responsive coverage" and "Minimum contrast" — both **found here, fixed here, re-measured here**. Two moderate findings fixed. Four minors recorded and left open with their reasons. Forms, Animations and Performance sections are N/A with the measurement that makes them N/A. **This row is a review, not a Lighthouse score**; the audits are row 8 and are cited nowhere in it. |
| 8 | Web-quality audit: no major unresolved | **PASS** | Was UNVERIFIED. `npx --yes lighthouse@13.4.1` → performance **1.00**, accessibility **1.00**, best-practices **1.00**, SEO 0.90; LCP **675 ms**, CLS **0**, TBT **0 ms**. `npx --yes @axe-core/cli@4.13.0` → **0 violations** (from 3 violations over 6 nodes, one `color-contrast` at impact *serious*). Raw output: [`evidence/lighthouse.json`](evidence/lighthouse.json), [`evidence/axe.json`](evidence/axe.json). Three Lighthouse audits still score below 1 and none is major — `meta-description` (minor, real, open), `document-latency-insight` (no gzip on the audit's own throwaway static server), `forced-reflow-insight` (attributed to `[unattributed]` on a page that ships zero scripts). Each is explained in [`evidence/README.md`](evidence/README.md). |
| 9 | No unexplained console errors or failed requests | PASS | Re-measured with a retained producer. **0 console errors and 0 failed requests** at all three widths. One failed request did exist and is now gone: Lighthouse's Chrome logged `favicon.ico 404`, so the page carries `<link rel="icon" href="data:,">` and `errors-in-console` no longer appears among the failing audits. The page requests nothing else — `subresourceRefs` is that one inline icon. |
| 10 | Performance does not obstruct interaction | PASS | Both surfaces, both retained. **Browser**: TBT **0 ms**, max potential FID **16 ms**, CLS **0**, LCP 675 ms; keypress→focus 3–12 ms. **CLI**: `init` **111 ms** median of 7, `qa` **96 ms** median of 5, Node v22.22.2 on win32, each run spawning the real process into a fresh `mkdtemp` so interpreter startup is inside the number. The baseline's CLI figures were struck out in iteration 2 as ~4× too slow and were re-measured by a probe nobody kept; this run keeps the probe, inside `audit.mjs`. |
| 11 | Tests and build green | PASS | `npm test` → **20 pass, 0 fail**, exit 0, captured at [`evidence/npm-test.txt`](evidence/npm-test.txt). No build step. The near-vacuous version of this row (D4 — the script was `node bin/init.mjs --help`) was closed in wave 3; the suite now includes the two citation guards that were knocked out before being trusted. |
| 12 | Verified in the rendered app, not inferred from code | **PASS** | Was UNVERIFIED for want of a subject. Four improvements landed in `templates/gmail-magic-resend.html` this pass — viewport meta, button contrast, `<main>` landmark, inline icon — and each was measured in the rendered page **before and after** by the same producer, not read off the diff. The before numbers are in the iteration 3 entry of [PROMOTION_LOG.md](PROMOTION_LOG.md); the after numbers are the artifacts in [`evidence/`](evidence/). |

**Status: NOT PROMOTED** — 10/12 PASS, 2 FAIL, 0 UNVERIFIED.
